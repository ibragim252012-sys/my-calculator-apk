name: Build Flet APK

on:
  workflow_dispatch:  # Сборка запускается только когда ты сам нажмёшь кнопку

jobs:
  build:
    runs-on: ubuntu-latest  # Linux-сервер (быстро и бесплатно)

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet

      - name: Build APK
        run: flet build apk
        env:
          FLUTTER_VERSION: "3.41.7"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: build/app/outputs/apk/release/app-release.apk
